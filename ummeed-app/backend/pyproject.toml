[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Human-normal strictness: catch real bugs and unused imports, don't fight style nitpicks.
select = ["E", "F", "W", "I"]
ignore = ["E501"]
